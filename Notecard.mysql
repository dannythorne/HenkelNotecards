use HenkelNotecards;

# Create the Note card table.
drop table if exists Notecard;
create table Notecard(
	 Id int auto_increment primary key,
	   coords varchar(32),
	   Comment char,
	   Position int,
	   workId int);
